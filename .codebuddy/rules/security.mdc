---
description: Command boundary, project allowlist, secret handling, validation, and logging security rules.
alwaysApply: true
enabled: true
provider:
---

# 安全规则

> Canonical detailed rule: this file. Codex and Claude reach it through `AGENTS.md`.

## 命令与项目边界

- 浏览器端不能提交任意 shell 命令。
- 浏览器端不能提交任意工作目录。
- 浏览器端只能选择服务端配置的 `projectId`。
- 服务端只能启动 allowlisted agent：`codebuddy`、`codex`、`claude`。
- 项目路径必须在服务端配置中解析和校验，不能信任客户端传入的路径。
- 不要把 agent 退出后自动降级成通用 shell，除非用户明确接受这个安全边界变化。

## 密钥与敏感信息

- 禁止把 provider secret、API token、authorization header 或本地凭据暴露给浏览器代码。
- 禁止把 secret 写入测试、fixtures、docs、截图或提交文件。
- 不要记录完整 secret、authorization header 或原始 provider credential。
- 不要提交 `.env`、`.env.local` 或本地机器凭据。

## 数据与日志

- WebSocket 和 HTTP 边界处必须校验消息类型和关键字段。
- 错误响应不要暴露内部栈、secret 或调试细节。
- 日志中避免输出敏感字段；必要时先脱敏。

## Tuimux / PTY 边界

- `src/tuimux/client.ts` 是 tuimux socket 协议封装层；不要在其他模块散落 raw socket JSON。
- 只转发 xterm 输入到已存在的 pane。
- 新增 tuimux 消息类型时，先考虑是否会扩大远程命令执行面。
